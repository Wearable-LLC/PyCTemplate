#include <gtest/gtest.h>

// TODO: replace with your own test files and remove this placeholder.
// Each new test file should be registered in tests/CMakeLists.txt via add_gtest().

TEST(PlaceholderTest, AlwaysPasses) {
    SUCCEED();
}
